---

name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  call-test-workflow:
    uses: skrootne/workflows/.github/workflows/test.yml@main

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run echo REAL_TOKEN
        run: echo ${{ secrets.REAL_TOKEN }}
